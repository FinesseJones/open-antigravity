# Port for the MITM Server
PORT=8080

# Target URL for the proxy (defaults to localhost:3000 where the main app will eventually live)
PROXY_TARGET="http://localhost:3000"

# Note: The core Open-Antigravity application is under development.
